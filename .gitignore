# Ignore specific session files
user_session.session
new_session_name.session
user_session.session-journal

# Ignore all session files
*.session

# Ignore media directory
/media/

channels.json
config.json
/.gitignore

# Android / Buildozer build artifacts
/.buildozer/
/bin/
*.apk
*.aab
*.keystore

# NDK native libraries copied by the CI workflow (not tracked in git)
/libs/